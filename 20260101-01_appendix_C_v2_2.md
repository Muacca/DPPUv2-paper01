# 付録C：数値計算の詳細（Numerical Computation Details）

本付録では、パラメータスキャンと Type 分類に用いた数値手法・設定を記録する。

## C.1 スキャン設定

### C.1.1 パラメータ範囲

| パラメータ | Min | Max | 点数 | 刻み |
|---|---:|---:|---:|---|
| $V$ | 0.0 | 5.0 | 51 | 0.1（線形） |
| $\eta$ | -10.0 | 5.0 | 151 | 0.1（線形） |
| $\theta_{\mathrm{NY}}$ | 0.0 | 5.0 | 51 | 0.1（線形） |

### C.1.2 固定値

| パラメータ | 値 | 意味 |
|---|---:|---|
| $\kappa$ | 1.0 | 重力結合 |
| $L$ | 1.0 | $S^1$ 周長 |
| $r_{\min}$ | 0.01 | 探索下限 |
| $r_{\max}$ | $10^6$ | 探索上限 |

### C.1.3 カットオフ依存性

Type 判定は $(r_{\min},r_{\max})$ と bound-hit 判定幅 $\delta$ に依存する。  
本研究では、Type I/II と boundary-attached Type III を十分分離できる広い範囲を採用した。  
境界近傍の単一点変動より、広い領域で頑健に現れる位相依存構造を重視して解釈する。

### C.1.4 評価点総数

1 組（1 topology × 1 variant）あたり：

$$
51\times151\times51=392{,}751
$$

3 topology × 3 variant（全 9 組）で：

$$
3{,}534{,}759
$$

点を評価。

## C.2 極値探索アルゴリズム

主手法は `scipy.optimize.minimize_scalar`（Brent, bounded）。

| 項目 | 値 |
|---|---|
| method | `bounded` |
| bounds | $[0.01,10^6]$ |
| xatol | $10^{-8}$ |
| maxiter | 500 |

## C.3 Type 分類アルゴリズム

### C.3.1 手順

1. Brent 法で最小候補 $r_0$ を探索  
2. 解が得られない／境界に張り付く場合は Type III  
3. bound-hit 判定： $r_0<r_{\min}+\delta$ または $r_0>r_{\max}-\delta$ （ $\delta=0.02$ ）なら Type III  
4. 曲率判定： $d^2V_{\mathrm{eff}}/dr^2|_{r_0}\le0$ なら Type III  
5. 原点近傍勾配 $s_0=dV_{\mathrm{eff}}/dr|\_{r=r\_{\min}}$ で最終分類  
   - $s_0>0$：Type I  
   - $s_0\le0$：Type II

### C.3.2 数値微分

中心差分（ $h=10^{-5}$ ）を使用：

$$
\frac{dV}{dr}\approx\frac{V(r+h)-V(r-h)}{2h},
\qquad
\frac{d^2V}{dr^2}\approx\frac{V(r+h)-2V(r)+V(r-h)}{h^2}.
$$

## C.4 障壁高の計算

### C.4.1 Type I

$$
\Delta V=\max_{r\in[r_{\min},r_0]}V_{\mathrm{eff}}(r)-V_{\mathrm{eff}}(r_0)
$$

実装：

1. $[r_{\min},r_0]$ を対数間隔 100 点でサンプル  
2. 最大値 $V_{\mathrm{eff}}^{\max}$ を取得  
3. $\Delta V=V_{\mathrm{eff}}^{\max}-V_{\mathrm{eff}}(r_0)$

補足：

- $\Delta V\le0$ の場合は Type II へ再分類  
- $r_0<2r_{\min}$ の場合は $\Delta V$ を未定義扱い

### C.4.2 Type II

井戸深さとして

$$
\Delta V=V_{\mathrm{eff}}(r_{\min})-V_{\mathrm{eff}}(r_0)
$$

を定義。

## C.5 数値精度と不確かさ

### C.5.1 グリッド解像度由来

- $\Delta\eta\approx0.1$
- $\Delta V\approx0.1$
- $\Delta\theta_{\mathrm{NY}}\approx0.1$

解析境界と数値境界の一致はこの範囲で評価する。

### C.5.2 極値探索精度

Brent 法の収束誤差（ $\sim10^{-8}$ ）は、物理的に意味のある $r_0$ の 3〜4 桁精度に対して十分。

### C.5.3 微分刻み幅依存

$h=10^{-5}$ は打ち切り誤差と丸め誤差のバランスで選定。  
$10^{-4}\sim10^{-6}$ で有意差がないことを確認。

## C.6 出力形式

### C.6.1 CSV カラム

| カラム | 型 | 意味 |
|---|---|---|
| `V` | float | vector torsion |
| `eta` | float | axial torsion |
| `theta_NY` | float | NY 結合 |
| `type` | int | 1/2/3 |
| `r0` | float | 安定半径（Type III は NaN） |
| `Veff_min` | float | 極小値 |
| `delta_V` | float | 障壁高（Type I） |
| `log10_r0` | float | $\log_{10}(r_0)$ |
| `log10_deltaV` | float | $\log_{10}(\Delta V)$ |
| `status` | string | 収束状態 |

### C.6.2 命名規則

`dppu_scan_<topology>_<variant>_<mode>_<timestamp>.csv`

例：`dppu_scan_S3S1_FULL_MX_20260211_091303.csv`

## C.7 例外処理

### C.7.1 収束失敗

- 警告ログを残す  
- グリッド探索へフォールバック  
- 失敗時は Type III + `FAILED`

### C.7.2 オーバーフロー

- `inf`/`nan` を検出  
- Type III + `OVERFLOW`

### C.7.3 典型失敗率

- 収束失敗：$<0.1\%$
- オーバーフロー：$<0.01\%$
- 有効分類率：$>99.8\%$

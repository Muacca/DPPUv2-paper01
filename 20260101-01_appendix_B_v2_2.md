# 付録B：再現性と検証（Reproducibility and Verification）

本付録では、本論文の計算再現性を担保するためのエンジン仕様と検証手順をまとめる。

## B.1 エンジン仕様

### B.1.1 DPPUv2 Engine Core v3

本論文の計算は Python + SymPy による `DPPUv2 Engine Core v3` で実行した。

| 項目 | 仕様 |
|---|---|
| Engine | DPPUv2 Engine Core v3 |
| 言語 | Python 3.10+ |
| 記号計算 | SymPy 1.12+ |
| 数値計算 | NumPy, SciPy |
| 並列化 | multiprocessing（パラメータスキャン） |

### B.1.2 モジュール構成

- `DPPUv2_engine_core_v3.py`：記号計算コア  
- `DPPUv2_parameter_scan_v3.py`：パラメータスキャン  
- `DPPUv2_runner_*.py`：トポロジー別 runner  
- `DPPUv2_visualize_*.py`：可視化

## B.2 サニティチェック

各計算段階で以下を自動検証する。

### B.2.1 計量両立性

$$
\nabla_c\eta_{ab}
=\partial_c\eta_{ab}
-\Gamma^{d}{}_{ca}\eta_{db}
-\Gamma^{d}{}_{cb}\eta_{ad}
=0.
$$

直交 frame（ $\eta_{ab}=\delta_{ab}$ ）では

$$
\Gamma_{abc}+\Gamma_{bac}=0
$$

を確認。

### B.2.2 Riemann 反対称性（3段階）

1. 最後 2 添字： $R^{a}{}\_{bcd}=-R^{a}{}\_{bdc}$
2. 先頭 2 添字（添字を下げて）： $R\_{abcd}=-R\_{bacd}$
3. 添字対交換： $R\_{abcd}=R\_{cdab}$

### B.2.3 torsion 検証

- T1：完全反対称性  

$$
T^{(1)}\_{abc}=T^{(1)}\_{[abc]}
$$

- T2：トレース条件  

$$
T^{(2)\lambda}{}\_{\mu\lambda}=V\_\mu
$$


### B.2.4 NY 密度整合性

$$
N_{\mathrm{FULL}}=N_{\mathrm{TT}}-N_{\mathrm{REE}}
$$

を代表点で数値検証。

## B.3 検証ログ例

### B.3.1 $S^3 \times S^1$

- Riemann 反対称性：全条件 PASS  
- Ricci 出力： $R=2(-V^2r^2-9\eta^2+36)/(3r^2)$

### B.3.2 $T^3 \times S^1$

- Riemann 反対称性：全条件 PASS  
- Ricci 出力： $R=-2V^2/3-6\eta^2/R_1^2$

### B.3.3 $Nil^3 \times S^1$

- Riemann 反対称性：全条件 PASS  
- Ricci 出力： $R=(-4R^2V^2-36\eta^2-3)/(6R^2)$

## B.4 クロスバリデーション

### B.4.1 解析式と数値評価の比較

代表点で $V_{eff}^{(\mathrm{anal})}$ と $V_{eff}^{(\mathrm{num})}$ を比較し、機械精度レベルで一致。

| Topology | $(V,\eta,\theta_{\mathrm{NY}})$ | $r$ | 解析値 | 数値値 |
|---|---|---:|---:|---:|
| $S^3$ | $(2,-4,1)$ | 1.0 | -473.74 | -473.74 |
| $S^3$ | $(2,-4,1)$ | 2.0 | -631.65 | -631.65 |
| $T^3$ | $(2,-3,1)$ | 1.0 | 1263.31 | 1263.31 |
| $Nil^3$ | $(2,0.5,1)$ | 0.5 | -128.30 | -128.30 |

### B.4.2 極限ケース検証

- $\theta_{\mathrm{NY}}\to0$：純 EC 極限へ復帰  
- $\eta\to0,\ V\to0$：Levi-Civita（GR）極限へ復帰  
- $T^3$（ $C^{i}{}\_{jk}=0$ ）： $R\_{\mathrm{LC}}=0$ を再現

以上により、本文結果の記号計算・数値計算の整合性と再現性を確認した。
